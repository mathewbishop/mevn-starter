// Rename this file and define a model here.
const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const placeholder = new Schema({

});


const placeholder = mongoose.model("", bookSchema);

module.exports = placeholder;